/* Ejercicio No: 3
 Desarrolle un algoritmo que realice la sumatoria de los números enteros comprendidos entre el 1 y el 10,
es decir, 1 + 2 + 3 + …. + 10. */

let n = 0;
let suma = 0;

// Asignación contador
n = n + 1;

// Asignación acumulador
suma = suma + n;

if ((n = 10)) {
  console.log(`Suma de contador ${n}`);
}

// Otra forma
for (let index = 1; index <= 10; index++) {
  console.log(index);
}
